[package]
name = "janus-bytes"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "WHATWG URL parsing and MIME sniffing — the byte-level entry point of the pipeline."

[lints]
workspace = true

[dependencies]
